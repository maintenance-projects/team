spring:
  application:
    name: spacenx

  datasource:
    url: jdbc:postgresql://localhost:5432/gitnx
    username: root
    password: "ultari12#$"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

  mvc:
    hiddenmethod:
      filter:
        enabled: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8081
  servlet:
    session:
      timeout: 30m

spacenx:
  gitnx:
    base-url: http://localhost:8080
    api-base-url: http://localhost:8080/api
  uploads:
    base-path: ${user.home}/spacenx-uploads
    max-file-size: 10485760

logging:
  level:
    com.spacenx: DEBUG
